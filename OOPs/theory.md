Object - entities in the real world \
class - like a bluprint for these entities

# Acess Modifiers
these are keywords on how our data(properties) & methods are accessed
1. private - accesible inside the class
2. public - accesible to everyone
3. protected - method is accesible inside the class and to it's derived class  

by default, everything is default in c++
